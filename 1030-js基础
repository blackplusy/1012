1.复合数据类型
	a.常用内置对象
	b.日期对象
	c.全局对象
	d.数学对象
	e.字符串对象
	f.数组对象
		js中最基本的数据类型，封装了和数组相关的方法和属性
		房间1      房间2       房间3      房间4
		范冰冰     刘亦菲      林志玲     李冰冰
		创建数组的方式
			1>通过new的方式调用Array对象构造函数
				var a=new Array()
			2>通过构造函数传递元素为参数(创建带指定元素的数组)
				var a=new Array(10,20,'a','b')
			3>直接将元素放入[]中，元素通过逗号分隔开
				var a=[1,2,3,4]
			4>创建指定数组(元素个数)
				var a=new Array(3)
2.其他数据类型
	1>函数
	函数的角色有2种，一种是数据类型，一种是程序设计
	定义函数的方式
		function typename(arg)
		{
			this.arg=arg;
		}
	2>空值
	之前说的数据类型都有自己的内容，编程中还有一种:什么都没有(null)
	注意:一个=代表复制 两个=代表比较 三个=代表对象比较
	3>不确定数据类型
	undefined 表示一个变量什么都没得到，连空都没有
	在应用里面空和undefined实际意义相同
3.数据类型转换
	1>隐式数据类型转换
		程序运行时候会根据上下文需要自动将数据转换为另外一种数据类型的过程
	2>显式数据类型转换
		通常用 parseInt、parseFloat
=================================================================
1.常量和变量
	a.常量
		从始至终不能被改变的数据
		常量的种类：字符类型，布尔类型，数值类型，null等等
		"天儿不错"  字符常量
		077        数值常量
		true       布尔常量
	b.变量
		1>什么是变量
			程序中可以发生改变的值，专业说法是可读写内存单元、
			可以理解为装东西的容器，变量名字代表分配给他们的内存单元
		2>变量的定义方式
			var 变量名=值  定义变量
			变量名=值      赋值
			var 变量名     定义变量
			字符类型变量
				var str="hello"
			布尔类型变量
				var s=false
		3>变量命名规则
			变量名必须以字母或者_或者$开头，后面可以跟数字或者字符，不能使用特殊符号
			js对标识符大小写敏感
		4>变量的作用域
			作用域:有效范围，js变量作用域有全局和局部之分，变量名同名时候局部作用域优先于全局作用域

2.运算符和表达式
	a.表达式
		产生结果的式子，表达式可以作为参数传递给函数或者将表达式赋值给变量保存，表达式有很多类型
	b.操作数
		表达式中的常量或者变量
		(a+b)/c+d
	c.运算符
		表达式中起计算作用，在数学中就是加减乘除
		c1.算数运算符
			+
			-
			*
			/
			%
		c2.关系运算符
			比较两个操作数大于小于等于运算符，返回的结果布尔类型
			1>相等运算符，判断两个操作数是否相等
				如果相等返回真，如果不等返回假
			2>等同运算符
				== 并非严格意义上相等性判断，通过数据类型转换也可以返回真
				===严格意义上相等性判断，他们的值和数据类型必须一致才能返回真
			3>不等运算符
				使用!=返回一个布尔值判断两个操作数是否不相等,如果不相等返回真
			4>不等同运算符
				！== 效果和等同运算符正好相反，严格不相等返回真
			5>比较运算符
				两个操作数的比较，返回值只能是真或者假
				>
				<
				==
				>=
				<=
				!=
			6>in运算符
				in运算符检查对象是否有指定属性，通常数组对象中存在元素索引集合，集合中每个元素都是非负整数
			7>instanceof运算符
				返回一个布尔值，表明对象是某个类的实例
		c3.逻辑运算符
			1>逻辑与
				操作符&&,双目运算符,操作数被当成布尔类型，可以是变量也可以是常量
				&& 用法
				操作数1&&操作数2
			2>逻辑或
				操作符||，属于双目运算符，对两个操作数进行或运算，返回一个布尔值
			3>逻辑非
				操作符!,属于单目运算符，对操作数值取反，操作数可以是变量也可以是常量
		c4.其他运算符
			1>类型检测运算符
			js中每个数据都有数据类型，可以通过typeof获取到数据类型的名字
			typeof返回值
				Number
				String
				Boolean
				Object
				Function
			2>条件运算符
			可以genuine条件在两个语句中选择一个来执行，三目运算符
			条件表达式?语句1:语句2
			满足条件执行语句1，不满足条件执行语句2
			3>void运算符
			有些地方不需要返回值，比如js代码写入浏览器执行
			4>数组存取运算符
			5>delete运算符
			delete运算符可以删除对象的一个属性或者数组的一个元素，js对象属性可以动态添加，也可以delete删除
			6>this运算符
			准确说是一个关键字，可以理解为运算符，面向对象中可以返回当前对象的引用

3.控制语句
	分为循环和判断
	建议:流程图制作(visio processon)
	a>判断语句(条件判断)
	根据条件选择一个任务分支语句的同城，js提供if判断语句和switch判断语句
		1>if语句
			测试的是一个布尔类型表达式，如果结果为真则执行某段程序
			if ( 表达式 )        //条件判断语句
			{
				语句组;          //满足条件后所执行的代码
			}
			注意:表达式一定要有，语句组是可选，可以通过一条或者多条语句组成
			注意:if花括号一定是成对出现，不能嵌套
		2>if-else语句	
			if语句根据表达式的值，来决定是否执行某个任务，没有其他选择(只有2种选择)
			if ( 表达式 )
			{
				语句组;
			}
			else
			{
				语句组;
			}
		3>if-else-if语句
			当有多个可供判断条件时候单个if-else表达不完全，使用if-else-if实现多路判断
			if ( 表达式 )
			{
				语句组;
			}
			else if ( 表达式 )
			{
				语句组;
			}
			。。。。
			else
			{
				语句组;
			}
			注意:表达式一定要有，语句组可以使用一条或者多条语句实现
		4>switch语句
			使用if-else实现多路判断不易于维护，使用switch语句代替，在给定多个选择种选择一个复合条件的执行
			switch ( 表达式 )
			{
				case 标识1:
					语句组;
				case 标识2:
					语句组;
			}
